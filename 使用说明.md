# 电脑清理扫描器 v2 · 使用说明

## 它是什么
一个帮你回答「电脑里有什么、哪些能删、哪些要留」的小工具。
**只扫描、只出报告，绝不会删除任何文件**——删不删、删哪个，永远由你自己决定。

v2 升级：新增重复文件检测、历史对比、项目依赖目录统计、Docker/WSL/AI 缓存清理点、报告排序筛选、勾选导出清理脚本、图形界面（双击无黑框）、手机扫码看报告。

## 怎么用
**双击桌面图标**即可，弹出图形窗口：选好选项 → 点「开始扫描」→ 完成后「打开报告」或「手机扫码查看」。

- 勾选「全盘扫描」会扫描所有本地硬盘（较慢，机械盘可能十几分钟）
- 报告自动存到 exe 旁的 `scan_reports\日期时间\`，也可以自定义位置（见下）
- 同目录 `扫描日志.log` 记录每次运行过程，黑框没了看这里

## 报告（浏览器打开的 HTML，共 7 个标签页）
| 标签页 | 内容 |
|---|---|
| 💾 磁盘与对比 | 各分区容量/剩余 + **与上次扫描对比**（清理效果一目了然） |
| 🧽 清理点 | A/B/C 分级 + 体积 + 具体建议，可勾选导出清理脚本 |
| 🔁 重复文件 | 按内容哈希找出的重复文件组，每组保留哪个、删哪个副本 |
| 📦 项目依赖目录 | 所有项目里的 node_modules/venv/target 等 |
| 🪀 已安装程序 | 装了哪些软件、体积、卸载命令 |
| 🐘 大文件排行 | 占空间最大的文件 |
| 📁 文件夹体积 | 各目录大小排行 |

**交互功能**：点击列头排序；每页顶部搜索框实时筛选；清理点/依赖目录页可勾选项目，点底部按钮导出「清理脚本-请先审阅.bat」——脚本把选中文件夹**移入回收站（可撤销）**，运行前请打开审阅路径列表。

另有 6 份 CSV 明细（程序清单/清理点/大文件/文件夹体积/项目依赖目录/重复文件）可用 Excel 打开。

## 安全等级
- **A 级 · 放心清理**：缓存、临时文件
- **B 级 · 确认后清理**：可能含个人内容（下载、微信文件、AI 模型缓存等），删前看一眼
- **C 级 · 谨慎/仅了解**：系统关键/个人数据，只统计体积

## 自定义报告位置
```
电脑清理扫描器.exe --set-report-dir "E:\我的报告"    设置并记住
电脑清理扫描器.exe --reset-report-dir                恢复默认
```
也可手动改 exe 同目录的 `扫描设置.json`。

## 命令行模式（cmd 中运行 exe，适合进阶）
```
电脑清理扫描器.exe --full                全盘扫描
电脑清理扫描器.exe --min-size 2GB        大文件门槛
电脑清理扫描器.exe --dup-min 50MB        重复文件门槛
电脑清理扫描器.exe --no-duplicates       跳过重复检测
电脑清理扫描器.exe --serve               扫完启动手机查看服务
```

## 常见问题
- **想看更全的系统目录统计**：右键 exe「以管理员身份运行」
- **首次运行被 SmartScreen/杀毒拦截**：选「仍要运行/信任」（新打包 exe 正常现象）
- **手机查看**：手机与电脑连同一 WiFi，点「手机扫码查看」扫码即可，关闭程序自动停止服务
- **历史对比**：从 v2 开始，每次扫描会存 summary.json，第二次扫描起自动出现对比数据
